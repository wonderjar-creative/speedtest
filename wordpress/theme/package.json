{
  "name": "elevation-theme",
  "version": "0.1.0",
  "private": true,
  "description": "WordPress block theme for Elevation Design Studio demo",
  "scripts": {
    "composer:install": "composer install",
    "composer:update": "composer update",
    "composer:dump": "composer dump-autoload",
    "composer:prod": "composer install --no-dev --optimize-autoloader",
    "lint:php": "./vendor/bin/phpcs",
    "lint:php:fix": "./vendor/bin/phpcbf",
    "dev": "npm run composer:install && npm run composer:dump && npm run lint:php:fix",
    "build": "npm run composer:install && npm run composer:dump && npm run lint:php",
    "version:patch": "npm version patch --no-git-tag-version && npm run version:sync",
    "version:minor": "npm version minor --no-git-tag-version && npm run version:sync",
    "version:major": "npm version major --no-git-tag-version && npm run version:sync",
    "version:sync": "node scripts/sync-theme-version.js",
    "zip:theme": "npm run build && npm run composer:prod && rm -rf /tmp/elevation-theme && mkdir -p /tmp/elevation-theme && rsync -a --exclude='.git' --exclude='.vscode' --exclude='package*.json' --exclude='composer.*' --exclude='.phpcs.xml' --exclude='README.md' --exclude='CHANGELOG.md' --exclude='CLAUDE.md' --exclude='*.zip' --exclude='node_modules' --exclude='scripts' . /tmp/elevation-theme/ && cd /tmp && zip -r \"$OLDPWD/elevation-theme-$(node -p \"require('$OLDPWD/package.json').version\").zip\" elevation-theme && rm -rf /tmp/elevation-theme && cd - && npm run composer:install"
  },
  "author": "Denver Headless",
  "license": "GPL-2.0-or-later"
}
