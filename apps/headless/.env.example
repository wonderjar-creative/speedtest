# Shared defaults (non-sensitive)
# These are overridden by .env.development.local or .env.production.local
# Copy the appropriate .env.*.local.example → .env.*.local and fill in secrets

# Template Part Revalidation Time (in seconds)
TEMPLATE_PART_REVALIDATE=300

# WordPress Theme Name
WP_THEME="elevation-theme"
